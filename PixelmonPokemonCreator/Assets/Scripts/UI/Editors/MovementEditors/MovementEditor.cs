using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MovementEditor : MonoBehaviour
{
    [SerializeField] List<Selectable> selectablesToToggle_Rideable;
    [SerializeField] List<Selectable> selectablesToToggle_Fly;
    [SerializeField] List<Selectable> selectablesToToggle_Surf;

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
